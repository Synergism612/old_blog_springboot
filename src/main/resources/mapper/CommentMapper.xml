<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.blog.mapper.CommentMapper">

    <resultMap id="BlogCommentMap" type="com.project.blog.packag.BlogComment" >
        <id column="id" property="id" />
        <result column="content" property="content" />
        <result column="time" property="time"  />
        <result column="title" property="articleTitle"  />
        <result column="Icon" property="authorIcon" />
        <result column="name" property="authorName"  />
    </resultMap>

    <select id="getBlogIndexCommentList" resultMap="BlogCommentMap">
       SELECT
        `comment`.*,
		article.title,
		`user`.Icon,
		`user`.`name`
        FROM
        `comment`
        LEFT JOIN (article,`user` )
		ON `comment`.articleId = article.id AND `comment`.authorId = `user`.id
		ORDER BY `comment`.time desc
		Limit   0,6
    </select>

    <select id="getBlogIndexCommentListByArticleId" resultMap="BlogCommentMap">
       SELECT
        `comment`.*,
		article.title,
		`user`.Icon,
		`user`.`name`
        FROM
        `comment`
        LEFT JOIN (article,`user` )
		ON `comment`.articleId = article.id AND `comment`.authorId = `user`.id
		WHERE article.id  = #{articleId}
		ORDER BY `comment`.time desc
    </select>
</mapper>
